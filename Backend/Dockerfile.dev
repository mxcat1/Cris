FROM node:20-alpine

WORKDIR /app

# Toolchain por si alguna dep nativa (bwip-js, bcrypt, etc.) necesita compilar
RUN apk add --no-cache python3 make g++

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 3000

CMD ["npm", "run", "start"]
